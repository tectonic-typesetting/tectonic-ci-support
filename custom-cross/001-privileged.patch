diff --git a/src/docker.rs b/src/docker.rs
index 4ac2733..5c913a0 100644
--- a/src/docker.rs
+++ b/src/docker.rs
@@ -111,6 +116,7 @@ pub fn run(target: &Target,
 
     docker
         .arg("--rm")
+        .arg("--privileged")
         .args(&["--user", &format!("{}:{}", id::user(), id::group())])
         .args(&["-e", "XARGO_HOME=/xargo"])
         .args(&["-e", "CARGO_HOME=/cargo"])
